# ==============================================================================
# RAILWAY ENVIRONMENT VARIABLES
# ==============================================================================
# These variables are configured in Railway's dashboard UI, NOT used locally.
# This file is a template showing what needs to be set in Railway.

# ==============================================================================
# DATABASE - Railway PostgreSQL
# ==============================================================================
# Automatically set by Railway when you connect PostgreSQL service
DATABASE_URL=${{Postgres.DATABASE_URL}}

# ==============================================================================
# AWS CREDENTIALS - IAM User for S3 Access
# ==============================================================================
# Used by db-seed service to fetch questions from S3
# Create IAM user with S3 read access and generate permanent credentials
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
AWS_REGION=us-east-2
DATA_MIGRATIONS_BUCKET=blw-dataviz-everything

# ==============================================================================
# JWT AUTHENTICATION
# ==============================================================================
# Generate a secure random secret and use the same across all Railway services
JWT_SECRET=your-production-jwt-secret-here
JWT_EXPIRATION=24h

# ==============================================================================
# ADMIN USER (for seeding)
# ==============================================================================
# Used by db-seed service to create the admin user
ADMIN_EMAIL=your-admin@email.com
ADMIN_PASSWORD=secure-password-here

# ==============================================================================
# API CONFIGURATION
# ==============================================================================
PORT=3003
BATCH_UPDATE_INTERVAL_MS=3000
NODE_ENV=production
