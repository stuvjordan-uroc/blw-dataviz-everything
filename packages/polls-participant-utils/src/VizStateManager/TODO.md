# single split canvases

- **DONE** create FocusedCanvas class
- **DONE** add setClientDisplayMode(displayMode: "expanded" | "collapsed") method to FocusedCanvas
- **DONE** add setCanvasWidth(requestedWidth: number) method to FocusedCanvas
- **DONE** add subscribeToCanvasState(callback: () => void) FocusedCanvas
- **DONE** update to setServerState method of VizStateManager to update focused canvases with new state of split
- **DONE** update unit tests of computation functions because of new signatures
- unit test of computation functions in src/VizStateManager/SingleSplitCanvas

# unstyled canvas component and single split component?
