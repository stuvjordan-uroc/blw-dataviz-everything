/**
 * SegmentViz module - Visualization data generator for poll/survey responses
 * 
 * Exports the main SegmentViz class and all public types.
 */

export { SegmentViz } from './SegmentViz';

// Export public types
export type {
  VizConfigSegments,
  Point,
  PointPosition,
  ResponseGroupDisplay,
  SegmentWithPositions,
  SegmentVizView,
  ResponseQuestionVisualization
} from './types';
