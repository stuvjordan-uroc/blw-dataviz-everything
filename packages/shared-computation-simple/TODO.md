# finish statistics

Bot had partially filled out an efficient way to generate partial splits from basis splits laid out in a definite order. Then lost internet.

Need to get bot to finish the job. Then we can continue.

# add hydration/update function to segmentViz

should take an array of SplitWithStatsAndSegmentGroups and an array of RespondentData and return (1) updated SplitWithStatsAndSegmentGroups (2) diffs
